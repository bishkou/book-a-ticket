name: tests

on:
  pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - run: cd auth
      - name: Install dependencies
        run: npm install
      - run: npm run test:ci
